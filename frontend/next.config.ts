import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Next.jsのバージョンにより devIndicators の buildActivity オプションは
    型定義から削除されているため、設定を削除しました。
    デフォルトの挙動を使用します。
  */
};

export default nextConfig;